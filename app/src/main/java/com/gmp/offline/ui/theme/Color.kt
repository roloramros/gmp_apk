package com.gmp.offline.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta "energía renovable" para GMP — Gestión Montajes Pro.
// Verde (vegetación/sustentabilidad) + dorado solar (el sol, la fuente de
// energía) + azul cielo (el fondo contra el que se instalan los paneles).

// Verdes — color de marca principal
val SolarGreenDark = Color(0xFF0B4F3C)
val SolarGreenMid = Color(0xFF13795A)
val SolarGreen = Color(0xFF1E9E6F)
val SolarGreenLight = Color(0xFFDCF3E7)

// Dorado solar — acento (botones, ícono del sol, foco/estado activo)
val SolarAmber = Color(0xFFFDB813)
val SolarAmberDeep = Color(0xFFF08C1D)

// Azul cielo — para el degradado de fondo
val SolarSky = Color(0xFF2E86AB)
val SolarSkyDeep = Color(0xFF16577A)

// Neutros
val SolarBackground = Color(0xFFF3FAF6)
val SolarSurface = Color(0xFFFFFFFF)
val SolarOnSurfaceVariant = Color(0xFF5B6B63)
val SolarError = Color(0xFFD64545)
